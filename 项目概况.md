# 萌图 — UI/UX 审查项目概况

> 本文档供 UI/UX 专家审查时快速了解项目全貌。配合源码阅读效果更佳。

---

## 一、产品定位

**萌图** 是一款面向摄影爱好者的**摄影师全流程工作台**（纯本地 Android 应用，不联网）。

三个核心使用场景：

| 场景 | 时机 | 核心需求 |
|------|------|----------|
| **前期策划** | 拍摄前 | 创建拍摄策划（主题/风格/器材/shot list/参考图），可存模板复用 |
| **照片管理** | 拍摄后导入 | 批量导入 → 相册管理 → 标签分类 → 瀑布流浏览 |
| **调色参考** | 后期修图 | 查看影调/色卡/直方图，与参考图对比 |

**目标用户**：独立摄影师、摄影爱好者。照片以**套图**（9 张左右为一组）为主，单张照片较少。

---

## 二、信息架构（底部 4 Tab）

```
底部导航栏 [ 作品库 | 相册 | 策划 | 我的 ]
```

### Tab 1：作品库（全部照片瀑布流）
- **顶部**：标签 chips 筛选条（横向滚动，点击快速切换）
- **主体**：2 列瀑布流照片卡片
- **卡片右下角**：快速加标签图标（不进详情直接加标签）
- **长按卡片**：进入多选模式 → 底部操作栏（加入相册/加标签/取消）
- **右下角 FAB**：导入照片（系统相册选择器 → 导入方式弹窗）

### Tab 2：相册（相册列表）
- **主体**：相册卡片网格（封面照片 + 渐变叠名称/数量）
- **封面**：自动取首张照片（或手动设置）
- **长按相册**：编辑（名称+描述）/ 删除
- **FAB**：新建相册
- **点进相册**：3 列照片网格（套图浏览）+ 长按设封面/移除

### Tab 3：策划（拍摄策划列表）
- **顶部**：状态筛选 chips（全部/策划中/拍摄中/已完成/已归档）
- **主体**：策划卡片（标题/风格/状态/地点/日期）
- **FAB**：新建策划（表单：标题/主题/风格/地点/日期 + shot list 编辑器 + 器材清单）
- **内置模板**：人像外拍/街拍/静物
- **点进策划**：详情（shot list 完成度 + 器材 + 实拍照片网格）

### Tab 4：我的
- **顶部统计卡片**：照片数/标签数/相册数
- **功能入口**：标签管理 → 设置

---

## 三、详情页（渐进式披露）

照片详情页是核心分析页面，采用**三层渐进式披露**：

### 第一层（常驻顶栏）
- 返回 + 文件名 + 更多(⋮) + 删除
- **极简**：默认只显示照片 + 这 4 个按钮

### 第二层（激活工具栏）
- 当用户开启了黑白/Clipping/构图/取色等工具时，自动出现一行快捷开关
- 方便快速关闭正在使用的工具

### 第三层（"更多" BottomSheet）
- 点击 ⋮ 弹出 BottomSheet，列出所有工具：
  - 黑白滤镜（+ 对比度/曝光滑块）
  - Clipping 溢出警告
  - 构图参考线（三分法/黄金分割/对角线）
  - 取色器（长按拾色 + 持久化取色点）
  - 与其他照片对比
  - 添加标签
  - 加入相册

### 底部分析面板（AnimatedSize 展开/收起）
- 默认收起（52px 高度的标题栏），点击展开（340px）
- 5 个 Tab：直方图 / 色卡 / 影调 / 和谐度 / 取色
- 复用 `AnimatedSize` + `maxHeight` 切换（200ms easeInOut）

---

## 四、设计语言

### 暗房美学（Dark Room Aesthetic）
深炭灰背景让照片色彩跳出来，琥珀色（暗房安全灯色温）做点缀。

| Token | 色值 | 用途 |
|-------|------|------|
| **背景 base** | `#0F0F0F` | Scaffold 背景 |
| **背景 surface** | `#1A1A1A` | 卡片/面板背景 |
| **背景 elevated** | `#242424` | 悬浮元素 |
| **强调色 accent** | `#E8A838`（琥珀） | FAB/选中态/图标/进度条 |
| **文字 primary** | `#E8E8E8` | 主文字 |
| **文字 secondary** | `#9A9A9A` | 次要文字 |
| **文字 muted** | `#6A6A6A` | 辅助文字 |
| **分隔线** | `#2A2A2A` | 0.5px |

### 组件规范
- **圆角**：卡片 12 / Chip 8 / FAB 16 / SnackBar 8
- **间距**：面板内边距 12 / 通用 8/6/4
- **卡片**：elevation 0（纯色背景 + 圆角，无阴影）
- **Material 3**：`useMaterial3: true`
- **暗色优先**：`themeMode: ThemeMode.dark`（不支持浅色切换）

---

## 五、关键交互模式

### 1. 渐进式披露
- 详情页工具栏分层（常驻→激活→更多）
- 分析面板收起/展开（AnimatedSize）
- 标签快速操作（卡片图标 → 不进详情）

### 2. 长按多选
- 瀑布流长按 → 多选模式（选中蒙层 + 左上角勾选圈）
- 底部操作栏：加入相册/加标签/取消
- 相册详情页长按 → 设封面/移除

### 3. 标签筛选
- 作品库顶部横向 chips（全部 + 各标签）
- 点击切换筛选（复用 searchQueryProvider，200ms debounce）

### 4. 导入流程
- FAB → 权限预检 → 系统相册选择器 → 导入方式弹窗（直接/新建相册/已有相册）
- 导入时 SHA256 去重 + Isolate 生成缩略图

---

## 六、页面清单（14 个页面/组件）

| 页面 | 路径 | 功能 |
|------|------|------|
| MainShell | `main_shell.dart` | 底部 4 Tab 导航壳 |
| HomePage | `home_page.dart` | 作品库瀑布流 + 筛选 + 多选 + 导入 |
| DetailPage | `detail_page.dart` | 照片详情 + 分析面板 + 工具栏 |
| ComparePage | `compare_page.dart` | 双图对比分析 |
| AlbumPage | `album_page.dart` | 相册列表（封面卡片） |
| AlbumDetailPage | `album_detail_page.dart` | 相册详情（3 列网格 + 设封面） |
| PlanListPage | `plan_list_page.dart` | 策划列表 |
| PlanEditPage | `plan_edit_page.dart` | 策划创建/编辑（shot list 编辑器） |
| PlanDetailPage | `plan_detail_page.dart` | 策划详情 |
| ProfilePage | `profile_page.dart` | 我的（统计 + 入口） |
| SettingsPage | `settings_page.dart` | 设置（存储/缓存/版本） |
| TagManagePage | `tag_manage_page.dart` | 标签管理（分组显示） |
| PhotoCard | `photo_card.dart` | 瀑布流卡片（多选/快速标签） |
| AnalysisPanel | `analysis_panel.dart` | 分析面板（5 Tab + 展开收起） |

---

## 七、已知 UX 问题（待审查重点）

1. **4 Tab 导航在小屏上的空间**：底部 4 Tab + 作品库顶部筛选 chips，竖向空间紧张
2. **导入流程较长**：选图 → 导入方式弹窗 → 选相册（3 步），能否更流畅？
3. **策划页面的 shot list 编辑器**：当前是 ListTile + TextField，交互体验偏简陋
4. **相册详情页 3 列网格 vs 瀑布流**：相册用 3 列网格，作品库用瀑布流，视觉一致性？
5. **详情页工具栏的分层是否合理**：更多 BottomSheet 里的 7 个工具，认知负担？
6. **暗色主题唯一**：不支持浅色切换，白天户外使用可能反光
7. **空状态设计**：无照片/无相册/无策划时的引导是否足够清晰？
8. **批量操作的反馈**：多选 → 加入相册 → SnackBar，是否有更优雅的确认方式？

---

## 八、技术约束（影响 UI 的）

- **Flutter Material 3**：只能用 Material 组件（非 Cupertino）
- **Riverpod 3.x**：无 StateProvider，用 Notifier 管理状态
- **drift SQLite**：本地数据库，9 张表，所有数据在设备
- **纯本地**：无网络请求，无云同步，无社区分享
- **仅 Android**：arm64-v8a 单 ABI，最低 API 26（Android 8.0）
- **照片 ≤ 1000 张**：性能优化基准

---

## 九、审查建议关注点

1. **信息架构合理性**：4 Tab 的分组是否清晰？相册和策划是否应该合并？
2. **详情页工具栏**：7 个工具的分层和优先级是否合理？哪些可以合并/隐藏？
3. **导入流程**：能否从 3 步简化到 1-2 步？默认行为是什么？
4. **套图心智模型**：用户以 9 张为一组上传，瀑布流 vs 网格 vs 相册优先？
5. **策划页面交互**：shot list 编辑器能否更直观（如拖拽排序、卡片式）？
6. **视觉一致性**：圆角/间距/颜色 token 的使用是否统一？
7. **可访问性**：暗色主题下的对比度、字体大小、触摸目标尺寸
8. **空状态 + 引导**：首次打开 App 的引导流程
